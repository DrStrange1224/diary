<Window x:Class="Diary.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:Diary.Converters"
        mc:Ignorable="d"
        Title="Settings" Height="250" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        Background="{DynamicResource Window.Background}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis"/>
        <conv:LanguageToNameConverter x:Key="LangToName"/>
    </Window.Resources>

    <Grid Margin="16" TextElement.Foreground="{DynamicResource Text}">
        <StackPanel VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Strings.AppTheme}" FontSize="14"/>
                    <TextBlock Text="{Binding Strings.Light}" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="12,0,0,0"
                               Visibility="{Binding IsDarkTheme, Converter={StaticResource InverseBoolToVis}}"/>
                    <TextBlock Text="{Binding Strings.Dark}" FontSize="14" Foreground="{DynamicResource TextSecondary}" Margin="12,0,0,0"
                               Visibility="{Binding IsDarkTheme, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>

                <ToggleButton Grid.Column="1"
                              Width="46" Height="24"
                              VerticalAlignment="Center"
                              IsChecked="{Binding IsDarkTheme}"
                              Cursor="Hand">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="Track"
                                    Background="{DynamicResource Border}"
                                    BorderThickness="0"
                                    CornerRadius="12">
                                <Ellipse x:Name="Thumb"
                                         Width="18" Height="18"
                                         Fill="{DynamicResource TextSecondary}"
                                         HorizontalAlignment="Left"
                                         Margin="3"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Track" Property="Background" Value="{DynamicResource Accent}"/>
                                    <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                    <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </Grid>

            <Grid Margin="0,28,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding Strings.Language}"
                           FontSize="14"
                           VerticalAlignment="Center"/>

                <ComboBox Grid.Column="1"
                          Width="150"
                          ItemsSource="{Binding Languages}"
                          SelectedItem="{Binding SelectedLanguage}"
                          VerticalAlignment="Center"
                          Cursor="Hand">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Converter={StaticResource LangToName}}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </Grid>
        </StackPanel>
    </Grid>
</Window>